type: object
required:
  - intake
  - periodOfDay
  - program
properties:
  intake:
    allOf:
      - $ref: '../intake/intakeStaticProps.yml'
      - $ref: '../intake/intakeDynamicProps.yml'
  periodOfDay:
    $ref: '../periodOfDay/periodOfDay.yml'
  program:
    $ref: '../program/program.yml'
