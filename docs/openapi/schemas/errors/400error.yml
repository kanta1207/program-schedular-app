description: Invalid request error
content:
  application/json:
    schema:
      type: object
      required:
        - code
        - message
        - data
      properties:
        code:
          type: integer
          example: 400
        message:
          type: string
          example: Bad Request
        data:
          type: string
          nullable: true
          example: null